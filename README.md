# Simple Image Boundary Area Layer (SIBAL)

A learning-free, rule-based region proposal layer for object detection,
implemented in Rust + wgpu.

## Running

```bash
cd Simple_image_boundary_area_layer
cargo build --release
./target/release/Simple_image_boundary_area_layer
